[tool.poetry]
name = "tplinkmon"
# Versioning is managed by semantic-release (git tags + GHCR image tags).
# This field is a sentinel and intentionally not kept in sync with releases.
version = "0.0.0"
description = ""
authors = ["Fergus In London <fergus@fergus.london>"]

[tool.poetry.dependencies]
python = "^3.11"
tapo = "^0.9"
prometheus-client = "^0.13.1"
click = "^8.0.4"
loguru = "^0.7.0"
PyYAML = "^6.0"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
